[package]
name = "mock-provider"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"] # Crucial: WASM components must be cdylibs

[dependencies]
hydrust-sdk = { path = "../../crates/hydrust-sdk" }